# This workflow is managed centrally in nf-core/actions.
# Do not edit — all logic, updates and security fixes arrive automatically via the v1 tag.
name: Nextflow strict syntax lint
on:
  pull_request:
  push:
    branches: [master, dev]

permissions: {}

jobs:
  run:
    uses: nf-core/actions/.github/workflows/nextflow-lint.yml@v1
    permissions:
      contents: read
    secrets: inherit
